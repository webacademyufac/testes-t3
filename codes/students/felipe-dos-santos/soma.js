
function soma (param) {
    if(typeof(variavel.x)=="number" && typeof(variavel.y)=="number"  ){
        return (param.x + param.y)
    }else{
        return("TA ERRADO!!")
    }
   
}

let variavel = {
    x: '95',
    y: 30
}

module.export = soma
